# 前端优化任务计划

## 目标
优化前端代码质量，修复错误处理、AI请求超时、Toast通知等问题

---

## Phase 1: Toast 通知系统 [pending]
**目标**: 替换所有 `alert()` 为 Toast 通知

### 任务清单
- [ ] 安装 `sonner` 包
- [ ] 在 `src/app/layout.tsx` 添加 `Toaster` 组件
- [ ] 替换 `src/app/page.tsx` 中的 4 处 alert
  - L19: `alert("请上传 CSV 文件")`
  - L36: `alert(\`成功导入 ${data.count} 条记录\`)`
  - L39: `alert(data.error || "上传失败")`
  - L42: `alert("上传出错，请重试")`
- [ ] 替换 `src/app/dashboard/page.tsx` 中的 1 处 alert
  - L63: `alert("分析失败，请重试")`

**涉及文件**: `src/app/page.tsx`, `src/app/dashboard/page.tsx`, `src/app/layout.tsx`

---

## Phase 2: AI 请求超时处理 [pending]
**目标**: 为 AI API 调用添加超时机制，防止无限等待

### 任务清单
- [ ] 在 `src/lib/ai.ts` 添加 `AbortController` + timeout
- [ ] 在 `src/app/api/analyze/route.ts` 包装超时逻辑
- [ ] 在 `src/app/dashboard/page.tsx` 前端 fetch 添加超时
- [ ] 添加加载进度指示器

**涉及文件**: `src/lib/ai.ts`, `src/app/api/analyze/route.ts`, `src/app/dashboard/page.tsx`

---

## Phase 3: 错误状态 UI [pending]
**目标**: 添加独立的错误状态，区分网络错误和"不存在"

### 任务清单
- [ ] `src/app/report/[id]/page.tsx` 添加 `error` 状态
- [ ] `src/app/dashboard/page.tsx` 添加持久错误状态
- [ ] 显示友好的错误信息 UI

**涉及文件**: `src/app/report/[id]/page.tsx`, `src/app/dashboard/page.tsx`

---

## Phase 4: Error Boundary [pending]
**目标**: 为路由添加错误边界，防止整应用崩溃

### 任务清单
- [ ] 创建 `src/app/error.tsx`
- [ ] 创建 `src/app/dashboard/error.tsx`
- [ ] 创建 `src/app/report/error.tsx`

**涉及文件**: 新建 `error.tsx` 文件

---

## Phase 5: 上传部分失败提示 [pending]
**目标**: 告知用户部分记录导入失败

### 任务清单
- [ ] 修改 `src/app/api/upload/route.ts` 返回 `failedCount`
- [ ] 前端显示部分失败提示

**涉及文件**: `src/app/api/upload/route.ts`, `src/app/page.tsx`

---

## Phase 6: AI API 重试逻辑 [pending]
**目标**: 为临时网络故障添加重试机制

### 任务清单
- [ ] 在 `src/lib/ai.ts` 添加重试逻辑 (最多 3 次)
- [ ] 添加指数退避延迟

**涉及文件**: `src/lib/ai.ts`

---

## Phase 7: 路由导航优化 [pending]
**目标**: 使用 Next.js router 替代 window.location

### 任务清单
- [ ] `src/app/dashboard/page.tsx` 改用 `router.push()`

**涉及文件**: `src/app/dashboard/page.tsx`

---

## Phase 8: 单报告 API 端点 [pending]
**目标**: 创建专用端点提升效率

### 任务清单
- [ ] 创建 `src/app/api/reports/[id]/route.ts`
- [ ] 修改 `src/app/report/[id]/page.tsx` 使用新端点

**涉及文件**: 新建 API 路由, `src/app/report/[id]/page.tsx`

---

## Phase 9: 除零保护 [pending]
**目标**: 防止 sleepScore 全为 null 时除零

### 任务清单
- [ ] `src/app/api/analyze/route.ts:43` 添加保护
- [ ] `src/app/dashboard/page.tsx:97-98` 添加保护

**涉及文件**: `src/app/api/analyze/route.ts`, `src/app/dashboard/page.tsx`

---

## Phase 10: AI 响应验证 [pending]
**目标**: 验证 AI 返回的 JSON 结构

### 任务清单
- [ ] 添加 JSON schema 验证
- [ ] 字段缺失时记录警告

**涉及文件**: `src/app/api/analyze/route.ts`

---

## 优先级汇总

| 优先级 | Phase | 描述 |
|--------|-------|------|
| 🔴 高 | 1 | Toast 通知系统 |
| 🔴 高 | 2 | AI 请求超时处理 |
| 🔴 高 | 3 | 错误状态 UI |
| 🔴 高 | 4 | Error Boundary |
| 🟡 中 | 5 | 上传部分失败提示 |
| 🟡 中 | 6 | AI API 重试逻辑 |
| 🟡 中 | 7 | 路由导航优化 |
| 🟡 中 | 8 | 单报告 API 端点 |
| 🟢 低 | 9 | 除零保护 |
| 🟢 低 | 10 | AI 响应验证 |

---

## 错误记录
| 错误 | 尝试 | 解决方案 |
|------|------|----------|
| - | - | - |

---

## 备注
- 项目使用 shadcn/ui，已有 toast 组件基础
- AI 使用讯飞星火模型 (xfyunClient)
